// Defines the default timer duration in minutes for each session type
const timerConstants = {
  DEFAULT_WORK_SESSION_DURATION: 25,
  DEFAULT_SHORT_BREAK_DURATION: 5,
  DEFAULT_LONG_BREAK_DURATION: 25,
};

export default timerConstants;
